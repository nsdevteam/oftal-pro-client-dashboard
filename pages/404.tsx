import { NextPage } from 'next';

import { Typography } from '../elements';

const NotFoundPage: NextPage = () => (
  <Typography as="h1">404 - Página não encontrada</Typography>
);

export default NotFoundPage;
