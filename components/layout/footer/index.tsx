import { FC } from 'react';

import { Box, Typography } from '../../../elements';

const Footer: FC = () => (
  <Box as="footer">
    <Typography as="h1" m="L">
      Logo
    </Typography>
  </Box>
);

export default Footer;
